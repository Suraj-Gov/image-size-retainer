// const img = document.getElementById("unsplash");
// const width = img.clientWidth;
// const height = img.clientHeight;

// function calcAspRatioFit(width, height, maxWidth, maxHeight) {
//   var ratio = Math.min(maxWidth / width, maxHeight / height);
//   return {
//     width: width * ratio,
//     height: height * ratio,
//   };
// }

// console.log(calcAspRatioFit(width, height, 1920, 1000));

// it can be done with css only!
